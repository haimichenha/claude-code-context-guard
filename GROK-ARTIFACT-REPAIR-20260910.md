# Grok 执行与产物修复（2026-09-10）

## 已安装的修复
- Write 对 DOCX/PDF/DLL/AEX 等格式的文本伪装提前拒绝，并提示使用真实生成器；不恢复已损坏的旧文件。
- Bash 失败命令重放守卫：记录已观察的失败，再次原样请求先拒绝、继续重复则停止；成功修改允许重测，权限拒绝不由此授予权限。只保存哈希状态，30分钟失效；这是止损，不是进度判定器。
- UserPromptSubmit 从宿主事件注入真实 cwd，明确会话存储目录不是工程目录。
- 技能增加“使用适合需求的已测试工具 → 执行 → 固定验收 → 收尾”路径；提供精确短语加粗工具，不把其用途泛化成简历重排。

## 真实结果（不是模型自报）
1. 无路径锚定：误入会话目录，12轮上限，无产物。
2. 明确根目录：简单短语加粗产物通过5项宿主检查，但模型重复检查，12轮上限；执行流程不算成功。
3. 跨多个run的短语：模型自写代码未正确加粗，10轮上限；不接受。冻结旧验收在首个断言中断，其后项目尚未测到，不把这些项目误报为实际损坏。
4. 已测试工具辅助：Grok实际调用工具及验收，3次Bash、4轮正常结束，5项宿主检查通过，0次no-op拦截。工具算法由宿主编写并测试，不能说Grok独立实现了该算法。

机器可读摘要见 observations/grok-artifact-repair-20260910.json。原始会话保存在本机隔离目录，不上传。
本轮离线检查：context-guard 39项；通用技能40项（含8项可选python-docx测试），均通过。

## 安装
使用既有 install-runtime-guards.py，指定本地 AE-tool/grok-skills 根目录并加 --apply。会备份，只合并相关hook/规则与技能；保留窗口、模型、权限和凭据，不修改厂商可执行文件。完成后新开Claude会话加载规则。

Hook协议依据：https://code.claude.com/docs/en/hooks 。PostToolUseFailure的实际覆盖范围取决于客户端；本轮没有主动制造权限拒绝来宣称所有拒绝路径均已实测。

## 仍未证明
- 不承诺自由编程、复杂文档或长期无人值守稳定。工具辅助的单次成功不能覆盖此前失败。
- 没有修改、修复或渲染用户的真实简历；伪DOCX仍需要从真正的源文件恢复。
- 100万窗口和80%压缩配置保持原有选择；服务端百万token容量和实际压缩触发仍未做压力测试。
- 眩光Grok工具辅助构建尚未运行：向配置的外部网关发送二进制派生哈希/元数据被审批拦截，须补充明确授权。没有通过换路径/工具绕过。

## 窗口漂移的实际原因与修复
最后读回发现 autoCompactWindow 又被写成120万，而 settings.env 仍是100万/80%。
定位到本机 claude-stability-repair.ps1 在策略更新器之后再次强写120万，并把 env.cmd/env.ps1 写为120万/72%。
安装器现在仅在匹配旧代码时，把这一段委托给原策略更新器，不改进程清理部分，也未调用 KillOrphans。
补丁生成时发现换行转义错误，已从哈希校验备份恢复该文件，修正生成器并增加真实 PowerShell AST 回归后重装。
实际执行修复后的 stability -Quiet，settings、两份环境文件均保持100万/80%；再次本地 /autocompact 与 /context 查询确认1m，API耗时/费用均为0。
见 observations/native-runtime-1m-20260910.json。仍不代表服务端容量或实际长会话压缩触发已测试。
