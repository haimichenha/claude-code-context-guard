# Security Policy

This repository must not contain secrets.

Do not commit:

- API keys or tokens;
- cc-switch databases;
- Claude authentication files;
- OAuth/session/cookie data;
- full Claude Code `cli.js` bundles;
- command logs containing private output.

Keep only scripts, templates, snippets, and sanitized examples.
